import { QuizSessionQuestion } from './quiz-session-question';

describe('QuizSessionQuestion', () => {
  it('should create an instance', () => {
    expect(new QuizSessionQuestion()).toBeTruthy();
  });
});
